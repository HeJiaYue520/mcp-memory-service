# MCP Memory Service 小白开发流程说明

> 本文档记录了 MCP Memory Service 从零开始搭建本地 Web Dashboard + Cloudflare D1 混合存储的完整流程
> 版本: v8.62.9 | 更新时间: 2025-12-31

---

## 目录

1. [项目简介](#1-项目简介)
2. [环境准备](#2-环境准备)
3. [Cloudflare 配置](#3-cloudflare-配置)
4. [本地服务安装](#4-本地服务安装)
5. [启动服务](#5-启动服务)
6. [验证功能](#6-验证功能)
7. [智能自动存储 Hooks 集成](#7-智能自动存储-hooks-集成)
8. [常见问题解决](#8-常见问题解决)
9. [参考文档](#9-参考文档)

---

## 1. 项目简介

### 1.1 什么是 MCP Memory Service？

MCP Memory Service 是一个基于 Model Context Protocol (MCP) 的语义记忆服务，为 AI 助手提供持久化存储能力。

### 1.2 架构说明

```
┌─────────────────────────────────────────────────────────────┐
│                    Claude Desktop / AI 助手                  │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              MCP Memory Service (本地端口 8888)              │
│  ┌──────────────────┐         ┌──────────────────┐         │
│  │  SQLite-Vec      │◄────────┤  Cloudflare D1   │         │
│  │  (本地 5ms 读写)  │  同步    │  (云端持久化)     │         │
│  └──────────────────┘         └──────────────────┘         │
│           ▲                            ▲                    │
│           │                            │                    │
│  ┌────────┴────────────────────────────┴────────┐          │
│  │         Web Dashboard (FastAPI)               │          │
│  │    http://127.0.0.1:8888/                    │          │
│  └───────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 存储后端对比

| 后端类型 | 读写速度 | 离线可用 | 多设备同步 | 推荐场景 |
|---------|---------|---------|-----------|---------|
| **Hybrid** ⚡ | 5ms | ✅ | ✅ | **生产环境推荐** |
| SQLite-Vec | 5ms | ✅ | ❌ | 单机开发 |
| Cloudflare | 网络依赖 | ❌ | ✅ | 纯云端部署 |

---

## 2. 环境准备

### 2.1 系统要求

- **操作系统**: Windows 10/11, macOS, Linux
- **Python**: 3.10+
- **包管理器**: uv (推荐) 或 pip

### 2.2 安装 uv 包管理器

```powershell
# Windows (PowerShell)
irm https://astral.sh/uv/install.ps1 | iex

# 验证安装
uv --version
```

### 2.3 克隆项目

```powershell
# 克隆仓库
git clone https://github.com/doobidoo/mcp-memory-service.git
cd mcp-memory-service

# 或使用已存在的项目目录
cd C:\App\AIMCP\mcp-memory-service
```

---

## 3. Cloudflare 配置

### 3.1 创建 Cloudflare 账号

1. 访问 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 注册/登录账号

### 3.2 创建 D1 数据库

```bash
# 安装 Wrangler CLI
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 创建 D1 数据库
wrangler d1 create my_team_data_mcp-memory-d1
```

**重要**: 记录命令返回的 `database_id`，格式如：
```
database_id = "271a15d1-5d5b-4cac-b680-a79b19eec8e5"
```

### 3.3 创建 Vectorize 向量索引

```bash
# 创建向量索引 (768 维度，cosine 相似度)
wrangler vectorize create mcp-memory-index --dimensions=768 --metric=cosine
```

### 3.4 初始化 D1 数据库表结构

**⚠️ 关键步骤**: 数据库创建后默认没有表，需要手动创建。

```bash
# 创建 memories 表
wrangler d1 execute my_team_data_mcp-memory-d1 --remote --command="
CREATE TABLE IF NOT EXISTS memories (
    id TEXT PRIMARY KEY,
    content TEXT NOT NULL,
    metadata TEXT,
    embedding BLOB,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now')),
    is_deleted INTEGER DEFAULT 0
);"

# 创建 tags 表
wrangler d1 execute my_team_data_mcp-memory-d1 --remote --command="
CREATE TABLE IF NOT EXISTS tags (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    color TEXT,
    created_at TEXT DEFAULT (datetime('now'))
);"

# 创建 memory_tags 关联表
wrangler d1 execute my_team_data_mcp-memory-d1 --remote --command="
CREATE TABLE IF NOT EXISTS memory_tags (
    memory_id TEXT NOT NULL,
    tag_id TEXT NOT NULL,
    PRIMARY KEY (memory_id, tag_id),
    FOREIGN KEY (memory_id) REFERENCES memories(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);"

# 验证表创建
wrangler d1 info my_team_data_mcp-memory-d1 --remote
```

预期输出应显示 `num_tables: 3`

### 3.5 创建 API Token

1. 访问 [API Tokens 页面](https://dash.cloudflare.com/profile/api-tokens)
2. 点击 **Create Token**
3. 选择 **Create Custom Token**
4. 配置权限：

```
Account > D1 > Edit
Account > Vectorize > Edit
Account > Workers AI > Edit
Account > Account Settings > Read
```

5. **Account Resources**: 选择你的账号
6. 点击 **Continue to summary** → **Create Token**
7. **复制并保存 Token** (只显示一次!)

---

## 4. 本地服务安装

### 4.1 配置环境变量

创建 `.env` 文件在项目根目录：

```bash
# C:\App\AIMCP\mcp-memory-service\.env

# Cloudflare 配置
CLOUDFLARE_ACCOUNT_ID=7e6529ba606faa5fd30b39286e7385b2
CLOUDFLARE_API_TOKEN=G4ZQj-3Fr6Ia2zTd6By2Fz9rEVypTIpkckcdxODX
CLOUDFLARE_D1_DATABASE_ID=271a15d1-5d5b-4cac-b680-a79b19eec8e5
CLOUDFLARE_VECTORIZE_INDEX=mcp-memory-index

# 存储后端配置
MCP_MEMORY_STORAGE_BACKEND=hybrid

# HTTP Web Dashboard 配置
MCP_HTTP_ENABLED=true
MCP_HTTP_PORT=8888
MCP_API_KEY=mem0ry-shared-key-2024

# OAuth 认证 (可选)
MCP_OAUTH_ENABLED=true
```

**⚠️ 注意**:
- 替换 `CLOUDFLARE_ACCOUNT_ID` 为你的账号 ID
- 替换 `CLOUDFLARE_API_TOKEN` 为你创建的 Token
- 替换 `CLOUDFLARE_D1_DATABASE_ID` 为你的数据库 ID

### 4.2 安装项目依赖

```powershell
# 使用 uv 安装 (推荐)
uv pip install -e .

# 或使用传统 pip
pip install -e .
```

### 4.3 验证安装

```powershell
# 检查版本
uv run memory --version

# 预期输出: MCP Memory Service 8.62.9
```

---

## 5. 启动服务

### 5.1 方法一：使用批处理脚本 (推荐)

项目已包含 `restart_service.bat` 脚本，可自动重启服务：

```batch
@echo off
echo ========================================
echo 重启 MCP Memory Service
echo ========================================
echo.

echo [1/3] 查找并停止运行中的服务...
for /f "tokens=5" %%a in ('netstat -ano ^| findstr :8888 ^| findstr LISTENING') do (
    echo 找到进程 PID: %%a
    taskkill /F /PID %%a 2>nul
)

echo.
echo [2/3] 等待 2 秒...
timeout /t 2 /nobreak >nul

echo.
echo [3/3] 启动 MCP Memory Service (Hybrid 模式)...
cd /d "%~dp0"
set MCP_MEMORY_STORAGE_BACKEND=hybrid
set MCP_HTTP_ENABLED=true
set MCP_HTTP_PORT=8888
set MCP_API_KEY=mem0ry-shared-key-2024

echo.
echo 配置信息:
echo   - 存储后端: Hybrid (本地 + Cloudflare)
echo   - HTTP 端口: 8888
echo   - 仪表盘: http://127.0.0.1:8888/#
echo.

echo 正在启动服务...
start "MCP Memory Service" cmd /k "uv run uvicorn mcp_memory_service.web.app:app --host 0.0.0.0 --port 8888"

echo.
echo ========================================
echo 服务重启完成！
echo ========================================
pause
```

**使用方法**:
```powershell
# 双击运行或在命令行执行
.\restart_service.bat
```

### 5.2 方法二：手动启动命令

```powershell
# 设置环境变量并启动
$env:MCP_MEMORY_STORAGE_BACKEND="hybrid"
$env:MCP_HTTP_ENABLED="true"
$env:MCP_HTTP_PORT="8888"
$env:MCP_API_KEY="mem0ry-shared-key-2024"

uv run uvicorn mcp_memory_service.web.app:app --host 0.0.0.0 --port 8888
```

### 5.3 启动成功标志

服务启动成功后，会看到类似输出：

```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8888
```

---

## 6. 验证功能

### 6.1 访问 Web Dashboard

打开浏览器访问:

```
http://127.0.0.1:8888/
```

![image-20251231182516980](C:\App\AIMCP\mcp-memory-service\image-20251231182516980.png)

### 6.2 健康检查

```powershell
# 基础健康检查
curl http://127.0.0.1:8888/api/health

# 详细健康检查 (需要认证)
curl -H "Authorization: Bearer mem0ry-shared-key-2024" http://127.0.0.1:8888/api/health/detailed
```

**预期输出**:
```json
{
  "status": "healthy",
  "version": "8.62.9",
  "storage": {
    "backend": "Hybrid (SQLite-vec + Cloudflare)",
    "primary_backend": "SQLite-vec",
    "secondary_backend": "Cloudflare",
    "total_memories": 2,
    "sync_enabled": true,
    "sync_status": {
      "is_running": true,
      "cloudflare_available": true
    }
  }
}
```

### 6.3 API 文档

访问 Swagger API 文档:

```
http://127.0.0.1:8888/api/docs
```

### 6.4 创建测试记忆

```powershell
# 使用 curl 创建记忆
curl -X POST http://127.0.0.1:8888/api/memories \
  -H "Authorization: Bearer mem0ry-shared-key-2024" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "MCP Memory Service 是一个语义记忆服务",
    "metadata": {"tags": ["文档", "测试"]}
  }'
```

### 6.5 搜索记忆

```powershell
# 语义搜索
curl -X POST http://127.0.0.1:8888/api/search \
  -H "Authorization: Bearer mem0ry-shared-key-2024" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "语义记忆",
    "limit": 5
  }'
```

---

## 7. 智能自动存储 Hooks 集成

### 7.1 什么是 Hooks？

MCP Memory Service 提供了 **Claude Code Hooks** 功能，可以在开发过程中自动存储和检索记忆，无需手动操作。

**核心功能：**
- 🧠 **Natural Memory Triggers v7.1.3** - 85%+ 触发准确率的智能记忆检索
- 💾 **自动存储** - SessionEnd Hook 自动保存会话中的关键信息
- 🔍 **智能检索** - SessionStart Hook 自动注入相关记忆
- ⚡ **性能优化** - 多层处理策略 (50ms → 150ms → 500ms)
- 🎯 **模式检测** - 自动识别决策/错误/学习/实现等模式

### 7.2 Hooks 架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Claude Code 会话                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  SessionStart Hook → 自动注入相关记忆                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                          │                                  │
│                          ▼                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Natural Memory Triggers → 智能记忆触发               │  │
│  │  - 多层处理: 50ms → 150ms → 500ms                    │  │
│  │  - 模式检测: 问题/决策/实现/学习                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                          │                                  │
│                          ▼                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  SessionEnd Hook → 自动存储会话记忆                    │  │
│  │  - 主题提取                                          │  │
│  │  - 决策记录                                          │  │
│  │  - 洞发现现                                          │  │
│  │  - 代码变更                                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 安装 Hooks

#### 7.3.1 前置要求

```powershell
# 检查 Claude Code CLI
claude --version

# 检查 Node.js (需要 14+)
node --version

# 检查 Python (需要 3.10+)
python --version
```

#### 7.3.2 安装命令

```powershell
# 进入 hooks 目录
cd C:\App\AIMCP\mcp-memory-service\claude-hooks

# 完整安装 (推荐 - 包含所有功能)
python install_hooks.py

# 仅安装基础 Hooks
python install_hooks.py --basic

# 仅安装 Natural Memory Triggers
python install_hooks.py --natural-triggers

# 仅安装自动捕获功能
python install_hooks.py --auto-capture
```

#### 7.3.3 安装输出示例

```
============================================================
Claude Code Memory Awareness Hooks Installer v8.62.9
============================================================

[INFO] Script location: C:\App\AIMCP\mcp-memory-service\claude-hooks
[INFO] Target hooks directory: C:\Users\YourName\.claude\hooks
[INFO] Platform: windows

============================================================
Prerequisites Check
============================================================

[SUCCESS] Claude Code CLI found: claude 2.x.x
[SUCCESS] Node.js found: v20.x.x (compatible)
[SUCCESS] Python 3.11 found (compatible)

============================================================
Installation Complete!
============================================================

[SUCCESS] Complete Claude Code memory awareness system installed
Features available:
  ✅ Session-start and session-end hooks
  ✅ Natural Memory Triggers with intelligent pattern detection
  ✅ Mid-conversation memory injection
  ✅ Smart Auto-Capture (PostToolUse for Edit/Write/Bash)
  ✅ Performance optimization and CLI management

CLI Management:
  node C:\Users\YourName\.claude\hooks/memory-mode-controller.js status
  node C:\Users\YourName\.claude\hooks/memory-mode-controller.js profile balanced
```

### 7.4 Hooks 配置

#### 7.4.1 配置文件位置

```
~/.claude/hooks/config.json
```

#### 7.4.2 默认配置

```json
{
  "codeExecution": {
    "enabled": true,
    "timeout": 8000,
    "fallbackToMCP": true,
    "pythonPath": "python"
  },
  "memoryService": {
    "protocol": "http",
    "http": {
      "endpoint": "http://127.0.0.1:8888",
      "apiKey": "mem0ry-shared-key-2024",
      "healthCheckTimeout": 3000
    },
    "defaultTags": ["claude-code", "auto-generated"],
    "maxMemoriesPerSession": 8,
    "enableSessionConsolidation": true
  },
  "naturalTriggers": {
    "enabled": true,
    "triggerThreshold": 0.6,
    "cooldownPeriod": 30000,
    "maxMemoriesPerTrigger": 5
  },
  "performance": {
    "defaultProfile": "balanced",
    "enableMonitoring": true,
    "autoAdjust": true
  }
}
```

**重要配置说明：**

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `endpoint` | Web Dashboard 地址 | `http://127.0.0.1:8888` |
| `apiKey` | API 密钥 | 与 `.env` 保持一致 |
| `triggerThreshold` | 触发阈值 (0-1) | `0.6` |
| `defaultProfile` | 性能配置文件 | `balanced` |

### 7.5 性能配置文件

Hooks 提供三种性能配置文件，可通过 CLI 动态切换：

#### 7.5.1 配置文件对比

| 配置文件 | 延迟 | 活跃层级 | 适用场景 |
|----------|------|----------|----------|
| **speed_focused** | <100ms | 仅即时 | 速度优先，最小记忆感知 |
| **balanced** ⭐ | <200ms | 即时+快速 | 通用开发（推荐） |
| **memory_aware** | <500ms | 全部 | 最大化上下文感知 |

#### 7.5.2 CLI 管理命令

```powershell
# 查看当前状态
node ~/.claude/hooks/memory-mode-controller.js status

# 切换配置文件
node ~/.claude/hooks/memory-mode-controller.js profile balanced

# 调整触发阈值
node ~/.claude/hooks/memory-mode-controller.js sensitivity 0.6

# 查看所有命令
node ~/.claude/hooks/memory-mode-controller.js --help
```

### 7.6 Hooks 工作流程

#### 7.6.1 SessionStart Hook

**触发时机：** Claude Code 启动新会话时

**执行流程：**
```
1. 检测当前项目目录
2. 分析 Git 历史 (最近 14 天，最多 20 条提交)
3. 提取关键词 (文件名、提交信息、标签)
4. 语义搜索相关记忆
5. 注入最相关的 3-5 条记忆
```

**输出示例：**
```
🧠 Memory Hook → Initializing session awareness...
📂 Project Detector → Analyzing mcp-memory-service
📊 Git Context → 10 commits, 3 changelog entries
🔑 Keywords → docs, chore, version, v8.22.0, fix
💾 Memory Search → Found 3 relevant memories
📝 Memory Injection → 3 memories loaded (recent: 2, fallback: 1)
```

#### 7.6.2 SessionEnd Hook

**触发时机：** 使用 `/exit` 命令或正常关闭 Claude Code 时

**注意：** `Ctrl+C` 不会触发 SessionEnd（会暂停而非结束会话）

**执行流程：**
```
1. 分析整个会话内容
2. 提取关键信息：
   - 主题 (Topics)
   - 决策 (Decisions)
   - 洞察 (Insights)
   - 代码变更 (Code Changes)
3. 自动生成标签
4. 存储到记忆数据库
```

**存储要求：**
- 最小会话长度：100+ 字符
- 最小置信度：> 0.1
- 会话整合已启用

#### 7.6.3 Natural Memory Triggers

**触发时机：** 会话过程中智能触发

**多层处理：**
```
第 1 层 (50ms)  → 即时模式匹配，快速响应
第 2 层 (150ms) → 语义分析，智能触发
第 3 层 (500ms) → 深度分析，全面检索
```

**模式检测：**
- 问题模式：`help`, `error`, `issue`, `problem`
- 决策模式：`decided`, `chose`, `selected`, `using`
- 实现模式：`implemented`, `added`, `created`, `built`
- 学习模式：`learned`, `discovered`, `found`, `realized`

### 7.7 验证 Hooks 安装

#### 7.7.1 检查安装文件

```powershell
# 检查核心 Hook 文件
dir $env:USERPROFILE\.claude\hooks\core

# 预期输出：
# session-start.js
# session-end.js
# mid-conversation.js
# memory-retrieval.js
```

#### 7.7.2 测试 Hooks

**测试 SessionEnd Hook：**
```
1. 启动 Claude Code
2. 进行一些对话 (超过 100 字符)
3. 输入 /exit 正常退出
4. 检查 Web Dashboard 是否有新记忆
```

**测试 SessionStart Hook：**
```
1. 确保数据库中有记忆
2. 启动 Claude Code
3. 查看是否有记忆注入的提示
```

#### 7.7.3 查看 Hook 日志

```powershell
# 查看 Hook 配置
cat $env:USERPROFILE\.claude\hooks\config.json

# 查看最近存储的记忆
curl -X POST http://127.0.0.1:8888/api/search/by-tag \
  -H "Authorization: Bearer mem0ry-shared-key-2024" \
  -H "Content-Type: application/json" \
  -d '{"tags": ["session-consolidation"], "limit": 5}'
```

### 7.8 常见配置调整

#### 7.8.1 修改 HTTP 端口

如果 Web Dashboard 使用不同端口：

```json
{
  "memoryService": {
    "http": {
      "endpoint": "http://127.0.0.1:9999"
    }
  }
}
```

#### 7.8.2 调整触发敏感度

```json
{
  "naturalTriggers": {
    "triggerThreshold": 0.7  // 更高阈值 = 更少触发
  }
}
```

#### 7.8.3 禁用某个 Hook

编辑 `~/.claude/settings.json`，移除对应的 Hook 配置。

### 7.9 Windows 特殊说明

#### SessionStart Hook 问题（已修复）

**✅ FIXED** (Claude Code 2.0.76+): SessionStart hooks 现在可以在 Windows 上正常工作。

无需任何变通方法，直接使用即可。

---

## 8. 常见问题解决

### 8.1 D1 数据库 400 错误

**症状**:
```
Failed to initialize D1 schema: 400 Bad Request
num_tables: 0
```

**原因**: 数据库已创建但表未初始化

**解决方案**:
```bash
# 手动创建表 (见 3.4 节)
wrangler d1 execute my_team_data_mcp-memory-d1 --remote --command="CREATE TABLE IF NOT EXISTS memories..."
```

### 8.2 Vectorize 403 Forbidden

**症状**:
```
Error checking vectorize index: 403 Forbidden
Authentication error
```

**原因**: API Token 缺少必要权限

**解决方案**:
1. 访问 https://dash.cloudflare.com/profile/api-tokens
2. 编辑你的 Token，添加权限:
   - **Account > Cloudflare Vectorize > Edit**
   - **Account > Cloudflare Workers AI > Edit**
3. 保存 Token 并更新 `.env` 文件

### 8.3 端口被占用

**症状**:
```
OSError: [Errno 48] Address already in use
```

**解决方案**:
```powershell
# Windows: 查找并终止占用端口的进程
netstat -ano | findstr :8888
taskkill /F /PID <进程ID>

# 或修改 .env 中的端口
MCP_HTTP_PORT=8889
```

### 8.4 认证失败

**症状**:
```json
{"detail": "Invalid authentication credentials"}
```

**原因**: API Key 错误或认证方式不正确

**解决方案**:
```powershell
# 确保使用 Bearer Token 格式
curl -H "Authorization: Bearer mem0ry-shared-key-2024" http://127.0.0.1:8888/api/health/detailed

# 不要使用 (错误方式):
curl -H "X-API-Key: mem0ry-shared-key-2024" http://127.0.0.1:8888/api/health/detailed
```

### 8.5 混合后端未生效

**症状**: 健康检查显示 `backend: "SQLite-vec"` 而不是 `Hybrid`

**原因**: 环境变量未正确加载

**解决方案**:
```powershell
# 方法 1: 在命令行中显式设置
$env:MCP_MEMORY_STORAGE_BACKEND="hybrid"

# 方法 2: 确保 .env 文件在项目根目录
# 方法 3: 使用 python-dotenv 加载
pip install python-dotenv
```

### 8.6 Hooks 相关问题

#### 8.6.1 SessionEnd Hook 没有触发

**症状**: 使用 `/exit` 后没有生成记忆

**原因检查**:
```powershell
# 检查会话长度是否足够 (> 100 字符)
# 检查置信度是否足够 (> 0.1)
```

**解决方案**:
```
1. 确保会话内容超过 100 字符
2. 使用 /exit 而不是 Ctrl+C (Ctrl+C 只是暂停会话)
3. 检查 Web Dashboard 服务是否运行
4. 查看 hooks 配置: cat ~/.claude/hooks/config.json
```

#### 8.6.2 SessionStart Hook 显示 "No relevant memories found"

**症状**: 启动 Claude Code 后显示多次 "MCP Fallback" 消息

**原因**: HTTP/HTTPS 协议不匹配

**解决方案**:
```powershell
# 1. 检查服务器使用的协议
# 查看 .env 文件中的 MCP_HTTPS_ENABLED 设置

# 2. 更新 hooks 配置文件
# 编辑 ~/.claude/hooks/config.json
{
  "memoryService": {
    "http": {
      "endpoint": "http://127.0.0.1:8888"  // 或 https:// 取决于服务器配置
    }
  }
}

# 3. 重启 Claude Code
```

#### 8.6.3 Hooks 连接超时

**症状**: SessionStart Hook 超时或连接失败

**原因**: 端口不匹配或服务未启动

**解决方案**:
```powershell
# 1. 确认服务正在运行
curl http://127.0.0.1:8888/api/health

# 2. 检查端口是否一致
# .env 文件端口 vs hooks config.json 端口

# 3. 检查防火墙设置
```

#### 8.6.4 Natural Memory Triggers 触发过于频繁

**症状**: 会话中频繁触发记忆检索

**解决方案**:
```powershell
# 调整触发阈值 (0-1, 越高越不敏感)
node ~/.claude/hooks/memory-mode-controller.js sensitivity 0.7

# 或切换到 speed_focused 配置文件
node ~/.claude/hooks/memory-mode-controller.js profile speed_focused
```

#### 8.6.5 Hooks 安装失败

**症状**: 安装脚本执行失败

**解决方案**:
```powershell
# 1. 检查前置条件
node --version  # 需要 14+
python --version  # 需要 3.10+

# 2. 以管理员权限运行 (Windows)
# 右键 PowerShell → "以管理员身份运行"

# 3. 手动安装
cd C:\App\AIMCP\mcp-memory-service\claude-hooks
python install_hooks.py --force
```

---

## 9. 参考文档

### 9.1 官方文档

- **GitHub 仓库**: [doobidoo/mcp-memory-service](https://github.com/doobidoo/mcp-memory-service)
- **Wiki 文档**: [项目 Wiki](https://github.com/doobidoo/mcp-memory-service/wiki)
- **安装指南**: [docs/installation](https://github.com/doobidoo/mcp-memory-service/tree/main/docs/installation)

### 9.2 核心配置文件

| 文件 | 说明 |
|-----|------|
| `.env` | 环境变量配置 |
| `restart_service.bat` | Windows 服务重启脚本 |
| `src/mcp_memory_service/web/app.py` | FastAPI Web 应用 |
| `src/mcp_memory_service/storage/cloudflare.py` | Cloudflare 存储后端 |

### 9.3 Cloudflare 资源

- **D1 数据库文档**: [https://developers.cloudflare.com/d1/](https://developers.cloudflare.com/d1/)
- **Vectorize 文档**: [https://developers.cloudflare.com/vectorize/](https://developers.cloudflare.com/vectorize/)
- **Workers AI 文档**: [https://developers.cloudflare.com/workers-ai/](https://developers.cloudflare.com/workers-ai/)

### 9.4 MCP 协议

- **MCP 规范**: [https://modelcontextprotocol.io/](https://modelcontextprotocol.io/)
- **Claude Desktop 配置**: [https://github.com/anthropics/claude-desktop](https://github.com/anthropics/claude-desktop)

---

## 附录 A: 完整配置示例

### A.1 .env 文件完整示例

```bash
# ==========================================
# Cloudflare 配置
# ==========================================
CLOUDFLARE_ACCOUNT_ID=7e6529ba606faa5fd30b39286e7385b2
CLOUDFLARE_API_TOKEN=G4ZQj-3Fr6Ia2zTd6By2Fz9rEVypTIpkckcdxODX
CLOUDFLARE_D1_DATABASE_ID=271a15d1-5d5b-4cac-b680-a79b19eec8e5
CLOUDFLARE_VECTORIZE_INDEX=mcp-memory-index

# ==========================================
# 存储后端配置
# ==========================================
# 可选值: hybrid, cloudflare, sqlite_vec
MCP_MEMORY_STORAGE_BACKEND=hybrid

# ==========================================
# HTTP Web Dashboard 配置
# ==========================================
MCP_HTTP_ENABLED=true
MCP_HTTP_PORT=8888
MCP_API_KEY=mem0ry-shared-key-2024

# ==========================================
# OAuth 认证 (可选)
# ==========================================
MCP_OAUTH_ENABLED=true

# ==========================================
# 性能优化 (可选)
# ==========================================
# SQLite 性能调优
MCP_MEMORY_SQLITE_PRAGMAS="journal_mode=WAL;synchronous=NORMAL;cache_size=-64000;temp_store=memory"

# 质量系统 (可选)
MCP_QUALITY_SYSTEM_ENABLED=true
MCP_QUALITY_AI_PROVIDER=none
MCP_QUALITY_BOOST_ENABLED=false

# ==========================================
# 日志配置 (可选)
# ==========================================
# MCP_LOG_LEVEL=INFO
```

### A.2 数据库表结构完整 SQL

```sql
-- memories 表
CREATE TABLE IF NOT EXISTS memories (
    id TEXT PRIMARY KEY,
    content TEXT NOT NULL,
    metadata TEXT,
    embedding BLOB,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now')),
    is_deleted INTEGER DEFAULT 0
);

-- tags 表
CREATE TABLE IF NOT EXISTS tags (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    color TEXT,
    created_at TEXT DEFAULT (datetime('now'))
);

-- memory_tags 关联表
CREATE TABLE IF NOT EXISTS memory_tags (
    memory_id TEXT NOT NULL,
    tag_id TEXT NOT NULL,
    PRIMARY KEY (memory_id, tag_id),
    FOREIGN KEY (memory_id) REFERENCES memories(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);

-- 创建索引 (可选，提升性能)
CREATE INDEX IF NOT EXISTS idx_memories_created_at ON memories(created_at);
CREATE INDEX IF NOT EXISTS idx_memories_is_deleted ON memories(is_deleted);
CREATE INDEX IF NOT EXISTS idx_tags_name ON tags(name);
```

---

## 附录 B: 故障排查命令速查

```powershell
# ==========================================
# 服务管理
# ==========================================
# 启动服务
.\restart_service.bat

# 查看服务状态
curl http://127.0.0.1:8888/api/health

# 停止服务 (查找并终止进程)
netstat -ano | findstr :8888

# ==========================================
# 数据库检查
# ==========================================
# 检查 D1 数据库信息
wrangler d1 info my_team_data_mcp-memory-d1 --remote

# 查询 D1 表数据
wrangler d1 execute my_team_data_mcp-memory-d1 --remote --command="SELECT COUNT(*) as count FROM memories"

# 检查本地 SQLite 数据库
sqlite3 data/memory.db "SELECT COUNT(*) FROM memories;"

# ==========================================
# 日志查看
# ==========================================
# 启用调试模式
$env:MCP_LOG_LEVEL="DEBUG"
uv run uvicorn mcp_memory_service.web.app:app --host 0.0.0.0 --port 8888

# ==========================================
# 配置验证
# ==========================================
# 验证环境变量
$env:MCP_MEMORY_STORAGE_BACKEND
$env:CLOUDFLARE_ACCOUNT_ID
$env:MCP_HTTP_PORT

# 验证 Python 环境
uv run memory --version
python --version
```

---

## 附录 C: Claude Desktop 集成 (可选)

如需在 Claude Desktop 中使用 MCP Memory Service:

### C.1 配置 Claude Desktop

编辑配置文件:

**Windows**:
```
%APPDATA%\Claude\claude_desktop_config.json
```

**macOS**:
```
~/Library/Application Support/Claude/claude_desktop_config.json
```

### C.2 配置内容

```json
{
  "mcpServers": {
    "memory": {
      "command": "uv",
      "args": [
        "--directory",
        "C:\\App\\AIMCP\\mcp-memory-service",
        "run",
        "memory",
        "server"
      ],
      "env": {
        "MCP_MEMORY_STORAGE_BACKEND": "hybrid",
        "CLOUDFLARE_ACCOUNT_ID": "7e6529ba606faa5fd30b39286e7385b2",
        "CLOUDFLARE_API_TOKEN": "G4ZQj-3Fr6Ia2zTd6By2Fz9rEVypTIpkckcdxODX",
        "CLOUDFLARE_D1_DATABASE_ID": "271a15d1-5d5b-4cac-b680-a79b19eec8e5",
        "CLOUDFLARE_VECTORIZE_INDEX": "mcp-memory-index"
      }
    }
  }
}
```

### C.3 使用 MCP 工具

在 Claude Desktop 中使用:
- `/memory-store` - 存储信息
- `/memory-recall` - 检索信息
- `/memory-health` - 健康检查

---

**文档结束**

> 本文档基于实际部署经验编写，涵盖了从零开始的完整流程。
> 如遇到问题，请参考 [GitHub Issues](https://github.com/doobidoo/mcp-memory-service/issues) 或查阅官方 Wiki。
